#!/usr/bin/env node

var crowler = require('../crowler.js');
crowler()
  .then(count => console.log(count > 0 ? `${count}件更新されました。` : "更新されていませんでした"))
  .catch(err => {
    if (err == 304) {
      console.log("前回の取得から、3分間は更新できません");
    } else {
      console.log(err);
    }
  });
